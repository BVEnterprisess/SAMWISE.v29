# Source Documents

Primary source material this project's specs were derived from. These are **inputs**, not
outputs — the specs in `../superpowers/specs/` cite them and may correct them, but must not
silently contradict them.

Expected here:

| File | Contents |
|---|---|
| `memory-spec-v1.4.md` | MEMORY SPECIFICATION v1.4 — the Memory Gravity Well, the force-balance control loop, the inline-skill execution flow |
| `omniroute-trace-inventory.md` | OmniRoute Observable Trace Inventory — ~506 enumerated traces across 40 categories |
| `omniroute-forensic-autopsy.md` | OmniRoute Forensic Architectural Autopsy — MVK, 12-factor scoring, 17 routing strategies, resilience stack |
| `yantrikdb-native-and-cei.md` | YantrikDB native capabilities (five indexes, LSM/DeltaIndex, `think()`) and the proposed Controlled Entropy Invariant |

A file listed above that is absent has not been committed yet. Do not treat a claim traced to a
missing document as verified — ask for the document.
